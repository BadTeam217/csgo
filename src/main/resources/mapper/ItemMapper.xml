<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sc.dao.ItemDao">
	<select id="findObjectById" resultType="com.sc.pojo.Item">
		select * from item where id=#{id}
		<!-- <where>
			<if test="id!=null and id!=''">
				id=#{id}
			</if>
		</where>
		and
		<where>
			<if test="type!=null and type!=''">
				type=#{type}
			</if>
		</where>
		and
		<where>
			<if test="skin!=null and skin!=''">
				skin=#{skin}
			</if>
		</where>
		and
		<where>
			<if test="quality!=null and quality!=''">
				quality=#{quality}
			</if>
		</where> -->
	</select>
</mapper>


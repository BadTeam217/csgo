spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/csgo?serverTimezone=UTC&useSSL=true&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: root
  thymeleaf: 
    prefix: classpath:/templates/pages/
    suffix: .html

#spring mybatis
mybatis:
  mapper-locations: classpath:/mapper/*/*.xml

#spring log
logging: 
  level: 
    '[com.sc]': debug

